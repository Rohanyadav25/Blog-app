<%- include("partials/header") -%>

    <h1>Home</h1>

    <p>
        <%= innerContent %>
    </p>

<% posts.forEach(function(post){ %>
     <h1><%= post.title %></h1>
     <!-- <p><%= post.content %></p> -->
     <p>
        <!-- <%= post.content.slice(0, 100) %><%= post.content.length > 100 ? "..." : "" %> -->
        <%= post.content.slice(0, 100) + "..."%> <a href="posts/<%= post.title %>">Read More</a>
      </p>
      
    <%})%>


 <%- include("partials/footer") -%>
